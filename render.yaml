services:
  - type: web
    name: sufuf-websocket
    env: node
    buildCommand: npm install
    startCommand: node server/index.ts
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /
    autoDeploy: false
    numInstances: 1
